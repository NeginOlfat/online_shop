import React from 'react';
import { Text } from 'react-native';

const Main = () => {
    return (
        <Text>
            Hello, this is main screen
        </Text>
    )
}

export default React.memo(Main);